-:
	@test cargo && cargo update --package url --precise 2.2.2 || echo "cargo command not available"
	@test cargo && cargo update --package form_urlencoded --precise 1.0.1 || echo "cargo command not available"
	@test cargo && cargo update -p once_cell --precise 1.13.1 || echo "cargo command not available"
	@test cargo && cargo update -p bzip2 --precise 0.4.2 || echo "cargo command not available"

# vim: set noexpandtab:
# vim: set setfiletype make